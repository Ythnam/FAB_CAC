import { SearchCardsMainComponent } from './search-cards-main.component';

describe('SearchCardsMainComponent', () => {
  let component: SearchCardsMainComponent;

  beforeEach(() => {
    component = new SearchCardsMainComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
