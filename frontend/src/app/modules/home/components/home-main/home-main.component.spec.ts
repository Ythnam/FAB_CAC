import { HomeMainComponent } from './home-main.component';

describe('HomeMainComponent', () => {
  let component: HomeMainComponent;

  beforeEach(async () => {
    component = new HomeMainComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
